import { ActiveCompanyDirective } from './active-cerfifier.directive';

describe('ActiveCompanyDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveCompanyDirective();
    expect(directive).toBeTruthy();
  });
});
