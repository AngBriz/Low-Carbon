import { VehicleChasisPipe } from './vehicle-chasis.pipe';

describe('VehicleChasisPipe', () => {
  it('create an instance', () => {
    const pipe = new VehicleChasisPipe();
    expect(pipe).toBeTruthy();
  });
});
